1. Viết Logger chung cho toàn bộ bên ngoài
+ ghi log vào 1 file (mỗi lần restart Pi là 1 file khác nhau, còn restart app thì ko)
+ NÂNG CAO: quá bao nhiêu MB thì qua log mới dù ko restart Pi
+ NÂNG CAO: Giải nén file

2. Unit test dùng gg test (Có CmakeList riêng)
- Cho thằng coremgr là được
- Xài GMOCK + CmakeList cho nó chuẩn chuyên nghiệp.

3. HardwareManager mỗi 5s gửi nhiệt độ, tình trạng CPU, .. trạng thái của Pi 4. 
- Nếu chênh lệch quá 5% so với state cũ thì update + bắn tới CloundManager
PHAT TRIỂN MSG DBUS ĐỂ NHẬN DATA LÀ MỘT VECTOR KIỂU Array[16]=50 --> 16 là nhiệt độ (Enum) và ta sẽ biết được là nhiệt độ = 50 độ
- Data khả năng sẽ truyền kiểu vector [key]=value : tạo một enum key để toàn bộ nhận diện được type(key). Cái nào = -1 thì ko update. Còn value != -1 thì set type = value tương ứng

4. CloundManager, HardwareMânger -> queue

18. BluetoothManager

5. Cần có xử lý Timer Timeout ở CoreManager, tức nó phải nhận NOTI sau mấy giây ko thì nó sẽ tự động trả về App status false
và kill thằng service đó (Để nó start lại)

17. Viết AudioManager riêng cho việc phát âm thanh (detect card device loa để phát)
Cứ có th nào request chơi file audio thì spawn một thread / một process (CÁI NÀY QUAN TRỌNG, CẦN HỎI)
BỞI có thể cho chúng nó chơi âm thanh đè lên nhau được mà (Tức nhiều process / nhiều thread chạy đồng thời)

18. GStreamer, Camera, Capture, ... OpenCV...

////////////// DEBUG
Start record -> Stop record là lại bị lỗi như vậy. Kiểm tra. 
Remove hay cancel có bị làm sao đâu. Có thể là thằng cancel đang làm hỏng luồng gì đó.

Nov 26 21:59:55 raspberrypi coremanager[14895]: coremanager: /home/sang/raspberrypi/sysroot/usr/include/boost/beast/websocket/detail/soft_mutex.hpp:89: bool boost::beast::websocket::detail::soft_mutex::try_lock(const T*) [with T = boost::beast::websocket::stream<boost::asio::basic_stream_socket<boost::asio::ip::tcp> >::write_some_op<WebSocketSession::send(const std::string&)::<lambda(boost::beast::error_code, std::size_t)>, boost::asio::const_buffers_1>]: Assertion `id_ != T::id' failed.
Nov 26 21:59:55 raspberrypi systemd[1]: coremanager.service: Main process exited, code=killed, status=6/ABRT
Nov 26 21:59:55 raspberrypi systemd[1]: coremanager.service: Failed with result 'signal'.